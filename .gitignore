# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Local-only docs (test credentials etc — never commit, repo is public)
docs/*.local.md

# Ad-hoc screenshots dropped at repo root for debugging — may contain personal info, never commit
/image*.png

# Common ignores
node_modules
.next/
.DS_Store
tsconfig.tsbuildinfo
.vercel
.env*

.env*
